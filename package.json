{
  "name": "ethercast",
  "version": "1.0.0",
  "description": "EtherCast – a retrocausal on-chain affirmations ledger for humans and AI agents.",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js",
    "start": "node server.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.4.0",
    "hardhat": "^2.20.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "siwe": "^2.3.2",
    "node": "^25.2.1"
  }
}
